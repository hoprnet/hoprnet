{
  "name": "@hoprnet/hopr-website.hopr.swiss",
  "version": "1.17.0-alpha.0",
  "private": true,
  "scripts": {
    "start": "GENERATE_SOURCEMAP=false CI=false node ./node_modules/.bin/craco start",
    "build": "GENERATE_SOURCEMAP=false CI=false craco build",
    "test": "CI=true craco test",
    "eject": "craco eject",
    "format": "prettier --check ."
  },
  "dependencies": {
    "@craco/craco": "^5.6.4",
    "@hoprnet/assets": "^1.17.0-alpha.0",
    "classnames": "^2.2.6",
    "lodash": "^4.17.15",
    "node-sass": "^4.12.0",
    "react": "^16.9.0",
    "react-dom": "^16.9.0",
    "react-router-dom": "^5.0.1",
    "react-scripts": "3.2.0"
  },
  "devDependencies": {
    "prettier": "^2.0.5"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "prettier": {
    "printWidth": 120,
    "semi": false,
    "singleQuote": true,
    "quoteProps": "consistent",
    "arrowParens": "avoid"
  }
}
