name: HOPR Test

on:
  push:
    branches: ['**']
    paths-ignore:
      - 'docs/**'

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12]
        package: ['utils', 'core', 'ethereum', 'all']

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
          registry-url: https://registry.npmjs.org/
      - uses: actions/cache@v2
        with:
          path: 'node_modules'
          key: ${{ runner.os }}-modules-${{ hashFiles('yarn.lock') }}
      - name: Install dependencies
        run: |
          yarn
          node_modules/.bin/lerna bootstrap
          node_modules/.bin/lerna link 

      - name: Build (all)
        if: matrix.package == 'all' 
        run: |
          node_modules/.bin/lerna run build --ignore @hoprnet/hopr-core --ignore @hoprnet/hopr-ethereum

      - name: Test (all)
        if: matrix.package == 'all'
        run: |
          node_modules/.bin/lerna run test --ignore @hoprnet/hopr-core --ignore @hoprnet/hopr-ethereum

      - name: Build ${{ matrix.package }}
        if: matrix.package != 'all'
        run: |
          node_modules/.bin/lerna run build --include-dependencies --scope @hoprnet/hopr-${{ matrix.package }}
      - name: Test ${{ matrix.package }}
        if: matrix.package != 'all' 
        run: |
          node_modules/.bin/lerna run test --scope @hoprnet/hopr-${{ matrix.package }}

#      - name: Test Docker build hoprd
#        working-directory: packages/hoprd
#        run: docker build .
