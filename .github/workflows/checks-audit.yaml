---
name: Audit

on:
  merge_group:
    types: [checks_requested]
  workflow_call:
    inputs:
      source_repo:
        required: true
        type: string
      source_branch:
        required: true
        type: string

concurrency:
  group: ${{ inputs.source_branch || github.ref_name }}-audit
  cancel-in-progress: true

jobs:
  cargo-audit:
    name: Audit
    runs-on: self-hosted-hoprnet-bigger
    steps:
      - name: Run Audit
        uses: hoprnet/hopr-workflows/actions/nix-action@348f8878a2bc6e1f4f5be0e98ffc7b3f7a79cb67 # nix-action-v1
        with:
          source_branch: ${{ inputs.source_branch }}
          command: nix run .#audit
