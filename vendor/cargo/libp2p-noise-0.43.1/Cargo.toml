# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2021"
rust-version = "1.65.0"
name = "libp2p-noise"
version = "0.43.1"
authors = ["Parity Technologies <admin@parity.io>"]
description = "Cryptographic handshake protocol using the noise framework."
license = "MIT"
repository = "https://github.com/libp2p/rust-libp2p"

[package.metadata.docs.rs]
all-features = true
rustc-args = [
    "--cfg",
    "docsrs",
]
rustdoc-args = [
    "--cfg",
    "docsrs",
]

[dependencies]
bytes = "1"
curve25519-dalek = "4.0.0"
futures = "0.3.28"
log = "0.4"
multiaddr = "0.18.0"
multihash = "0.19.0"
once_cell = "1.18.0"
quick-protobuf = "0.8"
rand = "0.8.3"
sha2 = "0.10.7"
static_assertions = "1"
thiserror = "1.0.44"
x25519-dalek = "1.1.0"
zeroize = "1"

[dependencies.libp2p-core]
version = "0.40.0"
path = "../../core"

[dependencies.libp2p-identity]
version = "0.2.3"
features = ["ed25519"]

[dev-dependencies]
env_logger = "0.10.0"
futures_ringbuf = "0.4.0"

[dev-dependencies.quickcheck]
path = "../../misc/quickcheck-ext"
package = "quickcheck-ext"

[target."cfg(not(target_arch = \"wasm32\"))".dependencies.snow]
version = "0.9.2"
features = ["ring-resolver"]
default-features = false

[target."cfg(target_arch = \"wasm32\")".dependencies.snow]
version = "0.9.2"
features = ["default-resolver"]
default-features = false
