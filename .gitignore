node_modules
.DS_Store
/.bin

db

*.swp
.vercel

test/local-fixtures

# logs
*.log
logs.txt

# we need to specify the direct paths since act only supports strict git ignore checks
packages/ethereum/types/
packages/ethereum/lib/types/
packages/utils/lib/types/
/types/
.direnv/

# yarn v2 specific
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions
.pnp.*

# Typescript build info resulting from Typescript Project References
# see https://www.typescriptlang.org/docs/handbook/project-references.html
**/*.tsbuildinfo

# used only in release and should not be tracked
packages/hoprd/default-environment.json
packages/cover-traffic-daemon/default-environment.json

# CT database
**/ct.json
/scripts/vagrant/.vagrant/
