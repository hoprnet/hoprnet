{
  // Only typecheck Typescript files in `script` folder as
  // they are executed using ts-node
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["scripts/*.ts"],
  "references": [{ "path": "packages/core-ethereum" }, { "path": "packages/utils" }, { "path": "packages/connect" }]
}
