/**
 * Creates a directory if it doesn't exist.
 *
 * @example
 * ```javascript
 * createDirectoryIfNotExists('db/testnet') // creates `./db` and `./db/testnet`
 * ```
 * @param path
 */
export declare function createDirectoryIfNotExists(path: string): void;
