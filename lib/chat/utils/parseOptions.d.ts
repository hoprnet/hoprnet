import type { HoprOptions } from '../../src';
/**
 * Parses the given command-line options and returns a configuration object.
 *
 * @returns
 */
export declare function parseOptions(): Promise<HoprOptions>;
