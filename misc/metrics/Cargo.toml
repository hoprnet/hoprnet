[package]
name = "hopr-metrics"
version = "1.2.0"
authors = ["HOPR Association <tech@hoprnet.org>"]
edition = "2021"
description = "Rust module containing support for general hopr-metrics collection"
homepage = "https://hoprnet.org/"
repository = "https://github.com/hoprnet/hoprnet"
license = "GPL-3.0-only"

[lib]
crate-type = ["rlib"]

[dependencies]
prometheus = "0.13.4"
regex = { workspace = true }

[features]
default = []
js = []