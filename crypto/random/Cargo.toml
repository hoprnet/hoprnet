[package]
name = "hopr-crypto-random"
version = "0.2.2"
edition = "2021"
license = "GPL-3.0-only"

[features]
default = []
fixed-rng = []

[dependencies]
rand = { workspace = true }
generic-array = { workspace = true }
