[project]
name = "no-git-dependencies-check"
version = "0.1.0"
description = "Checks Cargo.toml files for disallowed git-based dependencies"
requires-python = ">=3.14.5"
dependencies = ["tomli>=2.4.1; python_version < '3.11'"]

[tool.uv]
package = false
